import { systemNote } from "./systemNote";
import { backupLog } from "./backupLog";
import { systemProfile } from "./systemProfile";

export const schemaTypes = [systemNote, backupLog, systemProfile];
