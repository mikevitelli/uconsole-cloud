#!/bin/bash
set -e

case "$1" in
    purge)
        # Only remove config and data on purge (dpkg --purge)
        rm -rf /etc/uconsole/
        rm -rf /opt/uconsole/
        rm -rf /var/lib/uconsole/
        echo "Purged uconsole-cloud config and data."
        ;;
esac
