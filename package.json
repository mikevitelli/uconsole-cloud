{
  "name": "uconsole-dashboard",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,studio -c blue,green \"npm run dev -w @uconsole/frontend\" \"npm run dev -w @uconsole/studio\"",
    "build": "npm run build -w @uconsole/frontend && npm run build -w @uconsole/studio"
  },
  "workspaces": ["frontend", "studio"],
  "overrides": {
    "sanity": "^3.72.1",
    "@sanity/types": "^3.72.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
